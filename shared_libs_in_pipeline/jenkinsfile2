@Library('Jenkins-pipeline-shared-libraries') _
pipeline {
    agent any
    stages {
        stage('Audit tools') {
            steps {
                auditTools2 message: 'This is demo 2 from Job 2'
            }
        }
    }
}
